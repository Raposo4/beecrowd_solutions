#include <stdio.h>
#include <math.h>

int main()
{
  double sal,sold;
  char name[32];
  scanf("%s",name);
  scanf("%lf",&sal);
  scanf("%lf",&sold);
  sold = sold*0.15;
  printf("TOTAL = R$ %.2lf\n",(sal+sold));
  return 0;
}
